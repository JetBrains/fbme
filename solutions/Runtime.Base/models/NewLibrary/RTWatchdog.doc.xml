<?xml version="1.0" encoding="UTF-8"?>  
<section xmlns="http://docbook.org/ns/docbook"
        xmlns:xi="http://www.w3.org/2001/XInclude"
        xmlns:xlink="http://www.w3.org/1999/xlink">
  <info>
    <author>
       <personname>
          <firstname></firstname>
          <surname></surname>
       </personname>
       <email></email>
    </author>
    <abstract><wysiwyg_a><p>This function block is used to access the built-in runtime watchdog functions</p><p /><p>A RESET-Event has to be fired before the adjusted expire time runs out.</p><p /><p>
  </p></wysiwyg_a></abstract>
  </info>
<wysiwyg><p>
    <strong>Temporal Behavior</strong> </p><p>'INIT'-Event input: </p><p>- has to be triggered first to initialize, catches 'QI' and leads to an 'INITO'-Event.</p><p> </p><p>'START'-Event input:</p><p>- has to be triggered immediately after the initialization chain is ready; leads the time setting to output 'EXPIRETIME' and starts the FB.</p><p> </p><p>'RESET'-Event input:</p><p>- This input has to be connected to a FB sending events  triggered before the expiretime runs out. If not the selected action at input 'ACTION' starts.</p><p> </p><p>'STOP'-Event input:</p><p>- stops the RTWatchdog, no further watchdog action, Fires a 'STOPO' event.</p><p> </p><p>'TRIGGER'-Event input:</p><p>- A triggered 'TRIGGER'-Event fires the 'EXPIRED'-Event immediately, independently from expire time.</p><p> </p><p>'INITO'-Event output:</p><p>- This event is fired after each triggered 'INIT'.</p><p> </p><p>'STARTO'-Event output:</p><p>- This event is fired after a triggered 'START'.</p><p> </p><p>'RESETO'-Event output:</p><p>- This event is fired after each triggered 'RESET'.</p><p> </p><p>'STOPO'-Event output:</p><p>- This event is fired after each triggered 'STOP'.</p><p> </p><p>'EXPIRED'-Event output:</p><p>- This event is fired if expire time runs out and 'ACTION' is set on event (=2)</p><p> </p><p><strong>Functional Behavior</strong></p><p>'QI'-input:</p><p>- has to be TRUE, so that the block can be initialized and is ready to work. It can be set FALSE to deinitialize the block.</p><p> </p><p>'ACTION'-input:</p><p>- defines the action to be done if expire time runs out. </p><p>0 Default - same as Log</p><p>1 No action - no further action</p><p>2 Event - an event at 'EXPIRED'</p><p>3 Log - only message into log file/no event</p><p>4 Exit - stops runtime/no event</p><p>5 Reboot - reboots runtime</p><p> </p><p>'QO'-output:</p><p>- shows status of the block (TRUE is ready, FALSE is error or deinitialized).</p><p> </p><p>'EXPIRETIME'-output:</p><p>- shows the real expire time. It is advised to RESET the watchdog at least 3-5 times so fast to cover possible execution delays, Default setting is 30s; to set it to another time it is configurable in the *.config file: Configuration.FB.RTWatchdog.ExpireInterval = &lt;Milliseconds&gt;</p><p> </p><p>
  </p></wysiwyg></section>

