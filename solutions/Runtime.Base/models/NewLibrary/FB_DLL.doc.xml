<?xml version="1.0" encoding="UTF-8"?>  
<section xmlns="http://docbook.org/ns/docbook"
        xmlns:xi="http://www.w3.org/2001/XInclude"
        xmlns:xlink="http://www.w3.org/1999/xlink">
  <info>
    <author>
       <personname>
          <firstname>StanislavFirstname</firstname>
          <surname>Meduna</surname>
       </personname>
       <email>stanislav.meduna@nxtcontrol.com</email>
    </author>
    <abstract><wysiwyg_a><p>The FB_DLL implements an interface between a FB and arbitrary functions residing in a dynamically loaded library.</p><p>It is a generic function block. The count of event ports and data ports can be defined.</p><p>(More information about generic blocks: Studio help (press F1) at Solution→Distributed PAC Project→Editors→System Editor)</p><p>
</p><p /><p>
  </p></wysiwyg_a></abstract>
  </info>
<wysiwyg><p>An instance of this FB dynamically loads a DLL with its implementation at INIT. The DLL is then informed of a new FB instance using it.</p><p>A request calls into the DLL, giving the instance and input parameters as arguments. The implementation in the DLL can choose between processing the request synchronously, asynchronously at some later time or wake and do something entirely on its own. In any case it calls back into the FB instance, setting the output data and firing output events.</p><p>
</p><p /><p>
  </p></wysiwyg></section>

