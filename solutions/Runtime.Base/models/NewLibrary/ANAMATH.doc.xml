<?xml version="1.0" encoding="UTF-8"?>  
<section xmlns="http://docbook.org/ns/docbook"
        xmlns:xi="http://www.w3.org/2001/XInclude"
        xmlns:xlink="http://www.w3.org/1999/xlink">
  <info>
    <author>
       <personname>
          <firstname></firstname>
          <surname></surname>
       </personname>
       <email></email>
    </author>
    <abstract><wysiwyg_a><p>This function block calculates the minimum/maximum and average values of [len] numerical input values.</p><p>It is a generic block. Input and output datatypes (except input 'len') can be defined. The number of inputs and outputs is static.</p><p>(More information about generic blocks: Studio help (press F1) at Solution→Distributed PAC Project→Editors→System Editor)</p><p>
</p><p /><p>
  </p></wysiwyg_a></abstract>
  </info>
<wysiwyg><p>
    <strong>Temporal Behavior</strong> </p><p>'RESET'-Event input: </p><p>- Clears the internal list</p><p>- Set outputs to zero: 'tAvg${i}', 'tMax${i}', 'tMin${i}' and 'tDeadT${i}'</p><p>- Fires an 'RESET0'-Event</p><p> </p><p>'REQ'-Event input: </p><p>- Reads the 'len'-input, updates the internal list size</p><p>- Reads the 'pv${i}'-input</p><p>- If the list is empty then</p><p>    - Copy the input value of 'pv$' to all 'len' elements in the list</p><p>- Adds the input value to the list</p><p>- If the number of values in the list is equal to 'len'</p><p>    - Remove the oldest value and pass it to 'tDeadT${i}'-output</p><p>    - Add new value to the list</p><p>- Else</p><p>    - Add new value to the list</p><p>- Perform calculation of the minimum/maximum and average value</p><p>- Pass results to the corresponding outputs: 'tAvg${i}', 'tMax${i}', 'tMin${i}' </p><p>- Fire a 'CNF'-Event</p><p> </p><p><strong>Functional Behavior</strong></p><p>The value of the 'len'-input defines the number of the elements the list can hold and also used for all calculations. The first 'REQ'-Event copies the value of the 'pv$'-input to every element in the list of 'len' entries. So the first average value calculated will be equal to the first input value. </p><p>At every 'REQ' event the calculation of the average 'tAvg1', maximum value 'tMax' and minimum value 'tMin' are performed. The output 'tDeadT$' will be updated when the list is full and a new value is added. </p><p>
</p><p /><p>
  </p></wysiwyg></section>

