<?xml version="1.0" encoding="UTF-8"?>  
<section xmlns="http://docbook.org/ns/docbook"
        xmlns:xi="http://www.w3.org/2001/XInclude"
        xmlns:xlink="http://www.w3.org/1999/xlink">
  <info>
    <author>
       <personname>
          <firstname></firstname>
          <surname></surname>
       </personname>
       <email></email>
    </author>
    <abstract><wysiwyg_a><p>This function block provides a cyclic event generator with a configurable period which can be started and stopped.</p><p><em>For cyclic events which require a high-resolution timing or a phase parameter it is recommend to use the E_HRCYCLE function block.</em></p><p /><p>
  </p></wysiwyg_a></abstract>
  </info>
<wysiwyg><p>
    <strong>Temporal Behavior</strong> </p><p>'START'-event input: When a 'START'-Event is detected, the value of the 'DT'-input is captured and a cyclic timer starts. The 'EO'-Event is fired every time when the period, defined by 'DT', expires. Then the timer is restarted. It remains active until a 'STOP'-Event will be applied.</p><p>In the case when a timer is already active and a new 'START'-Event is applied then the event will be ignored.</p><p>'STOP'-event input: A 'STOP'-Event cancels a current active timer. It will be ignored when no timer is active.</p><p> </p><p><strong>Functional Behavior</strong></p><p>The 'DT'-input value is interpreted as the IEC-TIME duration format.</p><p>If the 'DT'-input is set to zero then the minimum cycle time is internally set to a default value. <em>Note:</em> A very small value generates a high frequency of output events which will significantly increase the processor load.</p><p /><p>
  </p></wysiwyg></section>

