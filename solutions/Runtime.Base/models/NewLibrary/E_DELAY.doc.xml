<?xml version="1.0" encoding="UTF-8"?>  
<section xmlns="http://docbook.org/ns/docbook"
        xmlns:xi="http://www.w3.org/2001/XInclude"
        xmlns:xlink="http://www.w3.org/1999/xlink">
  <info>
    <author>
       <personname>
          <firstname></firstname>
          <surname></surname>
       </personname>
       <email></email>
    </author>
    <abstract><wysiwyg_a><p>This function block passes an event to the output after a configurable delay. The delay generation can be started and stopped.</p></wysiwyg_a></abstract>
  </info>
<wysiwyg><p>
    <strong>Temporal Behavior</strong> </p>
<p>When a 'START'-Event is detected, the value of the 'DT'-input is captured and a delay-timer is started. When the timer expires then the 'EO'-Event is fired. The timer is NOT restarted. Only a new 'START'-Event activates the timer again with an ev. new 'DT'-input value.</p>
<p>In the case when a delay-timer is already active and a new 'START'-Event is applied then the event will be ignored.</p>
<p>The timer remains active until a 'STOP'-Event is detected. A 'STOP'-Event will be ignored when no timer is active.</p>
<p> </p>
<p><strong>Functional Behavior</strong></p>
<p>The 'DT'-input value is interpreted as the IEC-TIME duration format.</p>
<p>If the 'DT'-input is set to zero then the 'START'-Event is immediately passed through to the 'EO'-Event output within a minimum internal delay.</p></wysiwyg></section>

